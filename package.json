{
  "name": "cordova",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:cordova": "cd cordova && npm run build",
    "test": "react-scripts test --env=jsdom",
    "test:cordova": "cd cordova && npm run build && npm run test",
    "eject": "react-scripts eject"
  }
}
